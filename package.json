{
  "name": "named-port",
  "version": "0.0.0",
  "license": "MIT",
  "repository": "juliangruber/named-port",
  "description": "Deterministically map service names to port numbers",
  "type": "module",
  "scripts": {
    "test": "standard --fix && node example.js"
  },
  "devDependencies": {
    "standard": "^17.0.0"
  }
}
