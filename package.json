{
  "name": "named-port",
  "version": "1.0.1",
  "license": "MIT",
  "repository": "juliangruber/named-port",
  "description": "Deterministically map service names to port numbers",
  "type": "module",
  "scripts": {
    "test": "standard --fix && node--test"
  },
  "bin": {
    "named-port": " node bin/named-port.js"
  },
  "devDependencies": {
    "standard": "^17.0.0",
    "test": "^3.3.0"
  }
}
