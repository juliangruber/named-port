{
  "name": "named-port",
  "version": "2.0.1",
  "license": "MIT",
  "repository": "juliangruber/named-port",
  "description": "Deterministically map service names to port numbers",
  "type": "module",
  "types": "index.d.ts",
  "scripts": {
    "test": "standard --fix && node--test"
  },
  "bin": "bin/named-port.js",
  "devDependencies": {
    "standard": "^17.0.0",
    "test": "^3.3.0"
  }
}
